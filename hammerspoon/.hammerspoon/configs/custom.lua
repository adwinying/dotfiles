--
-- custom.lua
-- Custom (Device-specific) Configs
--

local custom_config = {
  keybindings = {
    enable_modifier_overrides = true,

    hyper_override = "rctrl",

    hyper_override_docked = nil,
  },
}

return custom_config
